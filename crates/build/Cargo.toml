[package]
name = "build-site"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[[bin]]
name = "build-site"
path = "src/main.rs"

[dependencies]
leptos = { workspace = true, features = ["ssr"] }
