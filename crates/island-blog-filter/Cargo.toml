[package]
name = "island-blog-filter"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[[bin]]
name = "island-blog-filter"
path = "src/main.rs"

# Leptos CSR + wasm-bindgen deps added in Phase 4.
[dependencies]
